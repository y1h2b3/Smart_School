<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.smart.www.mapper.StaffMapper">

    <resultMap id="BaseResultMap" type="Staff">
        <id property="id" column="id" jdbcType="BIGINT"/>
        <result property="staffId" column="staff_id" jdbcType="INTEGER"/>
        <result property="name" column="name" jdbcType="VARCHAR"/>
        <result property="phone" column="phone" jdbcType="VARCHAR"/>
        <result property="location" column="location" jdbcType="VARCHAR"/>
        <result property="isActive" column="is_active" jdbcType="INTEGER"/>
        <result property="note" column="note" jdbcType="VARCHAR"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
        <result property="updateTime" column="update_time" jdbcType="TIMESTAMP"/>
    </resultMap>

    <sql id="Base_Column_List">
        id
        ,staff_id,name,
        phone,location,is_active,
        note,create_time,update_time
    </sql>
    <select id="searchByName" resultType="com.smart.www.pojo.Staff">
        select img as image,staff_id,name,sex,birth,phone,location,is_active,note from staff
        where name like concat('%',#{value},'%')
        <if test="orders != null">
            ORDER BY ${orders}
            <if test="isAsc">
                ASC
            </if>
            <if test="!isAsc">
                DESC
            </if>
        </if>
        limit #{size} offset #{offset}
    </select>
    <select id="searchByStaffId" resultType="com.smart.www.pojo.Staff">
        select img as image,staff_id,name,sex,birth,phone,location,is_active,note from staff
        where staff_id like concat('%',#{value},'%')
        <if test="orders != null">
            ORDER BY ${orders}
            <if test="isAsc">
                ASC
            </if>
            <if test="!isAsc">
                DESC
            </if>
        </if>
        limit #{size} offset #{offset}
    </select>
    <select id="searchByIsActive" resultType="com.smart.www.pojo.Staff">
        select img as image,staff_id,name,sex,birth,phone,location,is_active,note from staff
        where is_active like concat('%',#{value},'%')
        <if test="orders != null">
            ORDER BY ${orders}
            <if test="isAsc">
                ASC
            </if>
            <if test="!isAsc">
                DESC
            </if>
        </if>
        limit #{size} offset #{offset}
    </select>
    <select id="searchByLocation" resultType="com.smart.www.pojo.Staff">
        select img as image,staff_id,name,sex,birth,phone,location,is_active,note from staff
        where location like concat('%',#{value},'%')
        <if test="orders != null">
            ORDER BY ${orders}
            <if test="isAsc">
                ASC
            </if>
            <if test="!isAsc">
                DESC
            </if>
        </if>
        limit #{size} offset #{offset}
    </select>
    <select id="searchBySex" resultType="com.smart.www.pojo.Staff">
        select img as image,staff_id,name,sex,birth,phone,location,is_active,note from staff
        where sex like concat('%',#{value},'%')
        <if test="orders != null">
            ORDER BY ${orders}
            <if test="isAsc">
                ASC
            </if>
            <if test="!isAsc">
                DESC
            </if>
        </if>
        limit #{size} offset #{offset}
    </select>
    <select id="searchStaff" resultType="com.smart.www.pojo.Staff">
        select img as image,staff_id,name,sex,birth,phone,location,is_active,note,create_time,update_time from staff
        <where>
            <if test="name != null">
                and name LIKE CONCAT('%', #{name}, '%')
            </if>
            <if test="isOnline != null">
                and is_active LIKE CONCAT('%', #{isOnline}, '%')
            </if>
            <if test="id != null">
                and staff_id LIKE CONCAT('%', #{id}, '%')
            </if>
            <if test="location != null">
                AND location like concat('%', #{location}, '%')
            </if>
        </where>
        <if test="orders != null">
            ORDER BY ${orders}
            <if test="isAsc">
                ASC
            </if>
            <if test="!isAsc">
                DESC
            </if>
        </if>
        limit #{size} offset #{offset}
    </select>
    <select id="countStaff" resultType="java.lang.Integer">
        select count(*) from staff
        <where>
            <if test="name != null">
                and name LIKE CONCAT('%', #{name}, '%')
            </if>
            <if test="isOnline != null">
                and is_active LIKE CONCAT('%', #{isOnline}, '%')
            </if>
            <if test="id != null">
                and staff_id LIKE CONCAT('%', #{id}, '%')
            </if>
            <if test="location != null">
                AND location like concat('%', #{location}, '%')
            </if>
        </where>
    </select>

</mapper>
