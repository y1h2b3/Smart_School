# 宝塔面板部署说明

## 📋 部署前准备

### 1. 服务器环境要求

- **Java**: JDK 17
- **MySQL**: 5.7+ 或 8.0+
- **Redis**: 5.0+
- **内存**: 建议 2GB+

### 2. 宝塔面板安装组件

1. 安装 **Java项目管理器**
2. 安装 **MySQL**
3. 安装 **Redis**

## 🚀 部署步骤

### 步骤1: 上传jar包

1. 打包项目：`mvn clean package`
2. 找到生成的jar包：`target/SmartSchool-0.0.1-SNAPSHOT.jar`
3. 上传到服务器（建议路径：`/www/wwwroot/smart-campus/`）

### 步骤2: 导入数据库

1. 在宝塔面板创建数据库 `smart_campus`
2. 导入SQL文件：`smart_campus.sql`

### 步骤3: 配置Redis

1. 在宝塔面板启动Redis服务
2. 如果设置了密码，记录下来

### 步骤4: 在宝塔添加Java项目

#### 基本配置

- **项目名称**: SmartCampus
- **项目路径**: `/www/wwwroot/smart-campus/`
- **Jar包名称**: `SmartSchool-0.0.1-SNAPSHOT.jar`
- **端口**: `8718`

#### JVM参数

```
-Xms512m -Xmx1024m -XX:+UseG1GC -XX:MaxGCPauseMillis=200
```

#### 启动参数（重要！）

```
--spring.profiles.active=prod
--DB_HOST=localhost
--DB_PORT=3306
--DB_NAME=smart_campus
--DB_USERNAME=root
--DB_PASSWORD=你的数据库密码
--REDIS_HOST=127.0.0.1
--REDIS_PORT=6379
--REDIS_PASSWORD=你的Redis密码(如果有)
--SERVER_PORT=8718
```

**注意**:

- 将 `你的数据库密码` 替换为实际密码
- 如果Redis没有密码，删除 `--REDIS_PASSWORD` 参数
- 如果需要修改端口，同时修改 `--SERVER_PORT` 和宝塔面板的端口设置

### 步骤5: 配置反向代理（可选）

如果需要通过域名访问：

1. 在宝塔面板添加网站
2. 配置反向代理到 `http://127.0.0.1:8718`

## 🔍 常见问题

### 1. 启动失败

**检查日志**：

```bash
cd /www/wwwroot/smart-campus/logs
tail -f smart-campus.log
```

**常见原因**：

- 数据库连接失败（检查密码、端口）
- Redis连接失败（检查Redis是否启动）
- 端口被占用（更换端口）
- 内存不足（增加JVM内存或服务器内存）

### 2. 数据库连接失败

```bash
# 检查MySQL是否启动
systemctl status mysql

# 测试数据库连接
mysql -u root -p
```

### 3. Redis连接失败

```bash
# 检查Redis是否启动
systemctl status redis

# 测试Redis连接
redis-cli ping
```

### 4. 端口无法访问

```bash
# 检查防火墙
firewall-cmd --list-ports

# 开放端口（如果需要）
firewall-cmd --zone=public --add-port=8718/tcp --permanent
firewall-cmd --reload
```

## 📊 访问地址

- **应用首页**: `http://服务器IP:8718`
- **API文档**: `http://服务器IP:8718/doc.html`
- **Druid监控**: `http://服务器IP:8718/druid`（用户名/密码: admin/admin）

## 🔄 更新部署

1. 停止应用（在宝塔面板中停止）
2. 备份旧jar包
3. 上传新jar包
4. 启动应用

## 📝 环境变量说明

| 变量名 | 说明 | 默认值 | 必填 |
|--------|------|--------|------|
| DB_HOST | 数据库地址 | localhost | 是 |
| DB_PORT | 数据库端口 | 3306 | 是 |
| DB_NAME | 数据库名称 | smart_campus | 是 |
| DB_USERNAME | 数据库用户名 | root | 是 |
| DB_PASSWORD | 数据库密码 | 123456 | 是 |
| REDIS_HOST | Redis地址 | 127.0.0.1 | 是 |
| REDIS_PORT | Redis端口 | 6379 | 是 |
| REDIS_PASSWORD | Redis密码 | (空) | 否 |
| SERVER_PORT | 应用端口 | 8718 | 否 |

## ⚠️ 安全建议

1. **修改默认密码**: 修改Druid监控的默认用户名密码
2. **关闭API文档**: 生产环境建议关闭Knife4j文档（已在prod配置中设置）
3. **配置HTTPS**: 建议配置SSL证书
4. **定期备份**: 定期备份数据库和应用数据
5. **监控日志**: 定期查看应用日志，及时发现问题

## 📞 技术支持

如遇问题，请查看日志文件：

- 应用日志: `logs/smart-campus.log`
- 控制台日志: 宝塔面板的项目日志
